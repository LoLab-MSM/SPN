
from sys import argv
import re
import math

def real_time_permutation(node_list, index):
    node = tuple(node_list)
    

print math.factorial(3)

#for index in factorial(3)

#===============================================================================
# Boolean_model = open(argv[2], 'r')
# 
# node_list = []
# for line in Boolean_model:
#     if (line.rstrip('\n')[0:1] != "#" and (line.rstrip('\n')[-4:] == "True" or line.rstrip('\n')[-5:] == "False" or line.rstrip('\n')[-6:] == "Random")):
#         line_list = re.split('\s*=\s*', line.rstrip('\n'))
#         line_list = line_list[:-1]
#         for element in line_list:
#             node_list.append(element)
#===============================================================================

